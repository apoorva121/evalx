# ============================================
# DEVELOPMENT ONLY - DO NOT USE IN PRODUCTION
# ============================================
# This file is for local development only.
# In production, install packages properly via pip/poetry.

# Development Mode Flag - Required for ./evalx script to run
EVALX_DEV_MODE=true

# Python Path - Add evalx_sdk and evalx_cli to import path for development
# REMOVE THIS IN PRODUCTION - use proper package installation instead
PYTHONPATH=${PYTHONPATH}:${PWD}/evalx_sdk:${PWD}/evalx_cli

# EvalX Service Configuration (for CLI)
EVALX_SERVICE_URL=http://localhost:8000
EVALX_BEARER_TOKEN=test123  # Must match BEARER_SECRET in eval-service/.env
